sudo: required
services:
  - docker

before_install:
  - docker build -t scryptbrewer/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true scryptbrewer/docker-react npm run test

language: generic
